sonar.host.url=https://sonarqube.mari0.dev

# Each CI pipeline overrides sonar.projectKey, sonar.sources, and coverage
# paths via -D flags so analyses are fully independent per component.
# See .forgejo/workflows/ci/ci-*.yml for per-component configuration.
