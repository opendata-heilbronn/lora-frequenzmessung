services:
  timescaledb:
    image: timescale/timescaledb:latest-pg17
    ports:
      - 5432:5432
    environment:
        POSTGRES_USER: timescaledb
        POSTGRES_PASSWORD: password
    volumes:
      - db-storage:/var/lib/postgresql/data
  mosquitto:
    image: eclipse-mosquitto
    ports:
      - 1883:1883
    volumes:
      - "./docker/mosquitto.conf:/mosquitto/config/mosquitto.conf"
  grafana:
    image: grafana/grafana
    container_name: grafana
    restart: unless-stopped
    ports:
      - '3000:3000'
    volumes:
      - grafana-storage:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
  mossquito-dashboard:
    image: terdia07/mqttui
    environment:
      MQTT_PORT: ${BROKEN_PORT}
      MQTT_BROKER: ${BROKEN_HOST_DASHBOARD}
      MQTT_USERNAME: ${MQTT_USERNAME}
      MQTT_PASSWORD: ${MQTT_PASSWORD}
    ports:
      - "5001:5000"
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
  backend:
    build:
      context: ./backend
      dockerfile: docker/go/Dockerfile
    ports:
      - "3001:3001"
    environment:
      DB_DSN: postgres://timescaledb:password@timescaledb:5432/postgres?sslmode=disable
      TTN_APP_ID: ${TTN_APP_ID}
      TTN_API_KEY: ${TTN_API_KEY}
      SENSOR_PAX_PATH: /sensor-pax
    volumes:
      - ./sensor-pax:/sensor-pax:ro
    depends_on:
      - timescaledb
volumes:
  grafana-storage: {}
  db-storage: {}
